In the TTTokenSelectionScreen users can upload an image to personalize their token.

Instance Variables
	selectedIndex:		<Number>
	tokenList:		<Dictionary>

selectedIndex
	- indicates the currently selected player

tokenList
	- saves each player's token
